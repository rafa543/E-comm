import { StatusBar, StyleSheet } from "react-native"

export const styles = () => {
    return StyleSheet.create({
        container: {
            marginBottom: 10
        },
        text: {
            color: "#FB7181",
            fontSize: 14,
            fontWeight: 'bold'
        }
    })
}